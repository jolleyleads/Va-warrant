import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';test('source registry is verification-first',async()=>{const sources=JSON.parse(await readFile(new URL('./data/sources.json',import.meta.url),'utf8'));assert.ok(sources.length>=2);for(const s of sources){assert.match(s.source_url,/^https:\/\//);assert.equal(s.adapter_enabled,false);}});