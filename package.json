{
  "name": "va-warrant-watch",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "npm install --ignore-scripts --no-audit --no-fund",
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "pdf-parse": "1.1.1"
  },
  "engines": {"node": ">=20 <27"}
}
